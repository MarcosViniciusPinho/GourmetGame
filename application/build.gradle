plugins {
    id 'java'
}

group 'com.objective'
version '1.0-SNAPSHOT'

dependencies {
    implementation 'org.openjfx:javafx-swing:20'
    implementation project(':business')
}

task runStart(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.objective.Start'
}